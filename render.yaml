# Python 版本（如果需要）
python: "3.9"

# 构建命令
build:
  commands:
    - npm install -g pnpm@9.0.0
    - pnpm install
    - pnpm run build

# 启动命令
start:
  command: pnpm run start

# 环境变量
env:
  - key: NODE_ENV
    value: production
